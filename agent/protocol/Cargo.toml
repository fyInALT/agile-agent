[package]
name = "agent-protocol"
version = "1.0.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
agent-types = { path = "../types" }

[dev-dependencies]
serde_json = { workspace = true }
